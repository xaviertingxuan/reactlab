import React from 'react'
import "./button.css"

export const Button = () => {

  let count = 0;

  const handleclick = (e) => e.targert,textcontent = "ouch!😥"

  
  
  return (
    <button className='button' onClick={(e) => handleclick(e)}>Click me😊</button>
  )
}
